<Window x:Class="ModelViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModelViewer"
        xmlns:viewModels="clr-namespace:ModelViewer.MVVM.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewModels:MainViewModel />
    </Window.DataContext>

    <Window.InputBindings>

        <!--Model interaction-->
        <KeyBinding Key="W" 
                Command="{Binding KeyPressCommand}" 
                CommandParameter="{x:Static Key.W}" />
        <KeyBinding Key="A" 
                Command="{Binding KeyPressCommand}" 
                CommandParameter="{x:Static Key.A}" />
        <KeyBinding Key="S" 
                Command="{Binding KeyPressCommand}" 
                CommandParameter="{x:Static Key.S}" />
        <KeyBinding Key="D" 
                Command="{Binding KeyPressCommand}" 
                CommandParameter="{x:Static Key.D}" />
    </Window.InputBindings>

    <Border BorderThickness="1">

        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0, 0" EndPoint="1, 1">
            </LinearGradientBrush>
        </Border.BorderBrush>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="20" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Menu Grid.Row="0">
                <MenuItem Header="Файл">
                    <MenuItem Header="Открыть" Command="{Binding StartLoadingFileCommand}"></MenuItem>
                </MenuItem>
            </Menu>

            <ContentControl x:Name="ClientArea"
                        Grid.Row="1"
                        Focusable="True"
                        Margin="5"
                        Content="{Binding CurrentView}">
            </ContentControl>
        </Grid>
    </Border>
</Window>
